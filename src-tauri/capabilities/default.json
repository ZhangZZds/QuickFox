{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the QuickFox launcher window.",
  "windows": ["main", "settings"],
  "permissions": ["core:default", "opener:default"]
}
