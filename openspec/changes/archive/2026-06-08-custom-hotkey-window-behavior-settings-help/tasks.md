## 1. 快捷键配置与匹配

- [x] 1.1 使用 TDD 为 `HotkeyState` 增加可配置唤醒键匹配测试，覆盖 `Shift+Shift`、`Control+Space`、修饰键组合和无效输入
- [x] 1.2 扩展 Rust 配置模型，新增 `hotkey.wake_shortcut` 默认值、TOML 读写和校验测试
- [x] 1.3 将全局键盘事件映射扩展为可匹配组合键的结构，并让监听线程使用当前配置
- [x] 1.4 更新全局唤醒状态文案，使其显示当前快捷键和权限/冲突提示

## 2. 窗口与托盘行为

- [x] 2.1 使用 TDD 覆盖托盘菜单“显示/隐藏 QuickFox”路由到统一 toggle 行为
- [x] 2.2 实现托盘菜单文案和行为切换：隐藏时显示，聚焦时隐藏，并同步 `LauncherWindowState`
- [x] 2.3 增加启动器失焦隐藏行为和测试，确保设置窗口不受该行为影响
- [x] 2.4 实现设置窗口可靠打开：存在则 show/unminimize/focus，不存在则重建设置窗口
- [x] 2.5 补充 Windows 托盘常驻语义测试/配置检查，确认后台运行只依赖托盘入口和隐藏主窗口

## 3. 设置页快捷键录制与说明

- [x] 3.1 使用 TDD 为前端快捷键录制交互增加测试，覆盖组合键、双击 Shift、单修饰键拒绝
- [x] 3.2 在设置页外观与窗口分区增加全局唤醒键录制控件，并保存到配置草稿
- [x] 3.3 移除设置页“返回搜索”按钮，保存设置后保持设置窗口打开
- [x] 3.4 为索引、网页搜索、历史、命令安全、外观/窗口等关键字段增加问号帮助图标和 hover/focus tooltip
- [x] 3.5 更新设置页响应式 CSS，使设置窗口宽/窄/低高度下内容不重叠且保存入口可访问

## 4. 前后端契约与文档

- [x] 4.1 更新 `tauriClient` 类型和测试，覆盖 `hotkey` 配置、快捷键状态和窗口命令契约
- [x] 4.2 更新 macOS、Windows、Linux 手工验收文档，加入自定义快捷键、失焦隐藏、托盘常驻和设置可靠打开
- [x] 4.3 更新视觉验收记录或新增记录，覆盖设置页帮助提示和自适应布局

## 5. 验证

- [x] 5.1 运行前端格式化、lint、测试和构建
- [x] 5.2 运行 Rust rustfmt、clippy 和单元测试
- [x] 5.3 运行 `openspec validate custom-hotkey-window-behavior-settings-help --strict`
- [x] 5.4 按 verification-before-completion 汇总真实验证结果

## 6. 追加反馈修复

- [x] 6.1 修复设置页父窗口变大但内容不扩展的问题
- [x] 6.2 修复设置页 tooltip 被父面板或滚动容器裁切的问题
- [x] 6.3 将 tooltip 文案从功能解释改为带具体填写格式和示例的配置指导
- [x] 6.4 Windows 默认索引范围发现可用本地盘符，不只使用用户 profile
- [x] 6.5 增加系统噪音、缓存、构建产物和特殊系统文件的隐式排除规则
- [x] 6.6 优化索引扫描去重、排除匹配和局部失败继续扫描
