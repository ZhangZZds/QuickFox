## 1. 调试与复现基线

- [x] 1.1 记录 macOS 手工复现步骤，覆盖 QF-001 到 QF-011
- [x] 1.2 梳理当前窗口、托盘、搜索、历史和设置的数据流，标出每个 bug 的根因假设
- [x] 1.3 为无法自动化的桌面行为补充 macOS/Windows/Linux 手工验收清单

## 2. 窗口与托盘行为

- [x] 2.1 先为 `Shift+Shift` 显示、置前、隐藏状态机写 Rust 单元测试
- [x] 2.2 实现窗口显示/隐藏切换逻辑，并让托盘“显示 QuickFox”复用该逻辑
- [x] 2.3 先为托盘设置入口和快速窗口移除设置按钮写前端/Tauri contract 测试
- [x] 2.4 实现托盘菜单打开设置页，移除 Compact 启动窗口内设置按钮

## 3. 搜索索引、过滤和排序

- [x] 3.1 先为 macOS `.app` 内部排除、Windows `.exe`/`.lnk`、Linux `.desktop` 应用识别写测试
- [x] 3.2 实现跨平台应用结果类型识别和应用包内部噪音过滤
- [x] 3.3 先为不存在查询 `Openspec_123` 不返回 PyCharm 内部路径写回归测试
- [x] 3.4 实现普通模糊搜索最低相关性阈值，保留显式正则搜索行为
- [x] 3.5 先为应用程序 > 文件 > 目录排序规则写 Ranker 测试
- [x] 3.6 实现类型优先级排序，并保留匹配质量、路径深度和历史权重

## 4. 网页搜索

- [x] 4.1 先为 `g 1234` 和 `bd 1234` 查询解析写测试
- [x] 4.2 修改网页搜索 Provider 使用 `prefix query` 语法并更新默认配置
- [x] 4.3 先为网页搜索结果 Enter 打开 URL 并记录输入历史写测试
- [x] 4.4 实现网页搜索执行路径和用户可配置引擎保存/读取

## 5. 索引刷新与设置页

- [x] 5.1 先为设置页刷新索引 Tauri contract 写前端测试
- [x] 5.2 实现设置页手动刷新索引入口和刷新结果反馈
- [x] 5.3 先为增量刷新保留未变化路径历史信号写 Rust 测试
- [x] 5.4 实现基于路径状态比较的触发式增量刷新
- [x] 5.5 先为设置页分组渲染写 UI 测试
- [x] 5.6 将设置页按“搜索与索引”“网页搜索”“历史”“命令执行”“外观与窗口”分组

## 6. 输入历史

- [x] 6.1 先为文件、目录、命令、网页搜索执行后记录输入写 storage 测试
- [x] 6.2 先为未按 Enter、Esc 关闭、未执行结果不记录输入写测试
- [x] 6.3 将命令历史迁移为输入历史，默认保留最近 15 条
- [x] 6.4 先为上/下方向键召回输入历史写 UI 测试
- [x] 6.5 实现搜索框输入历史召回，并处理与结果列表导航的状态边界

## 7. 结果列表 UI

- [x] 7.1 先为空输入不显示结果区域留白写 UI 测试
- [x] 7.2 修复空输入和无结果状态的布局
- [x] 7.3 先为右键菜单靠近目标结果显示写 UI 测试
- [x] 7.4 修复动作菜单定位和关闭行为
- [x] 7.5 先为列表内部滚动且不显示页面级割裂滚动条写 UI/CSS 测试
- [x] 7.6 调整结果列表滚动容器和视觉样式

## 8. 验证与收口

- [x] 8.1 运行 `npm run test`
- [x] 8.2 运行 `npm run rust:test`
- [x] 8.3 运行 `npm run check`
- [x] 8.4 运行 `openspec validate fix-launcher-manual-qa-bugs --strict`
- [ ] 8.5 在 macOS 执行手工验收并记录结果
- [x] 8.6 更新 Windows/Linux 手工验收清单，标出需要真实机器确认的行为
